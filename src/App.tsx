import ListGroup from './components/ListGroup';

function App() {
  // JSX Javascript XML
  const name = '';
  if (name)
    return <h1>Hello {name}</h1>
  return <div><ListGroup /></div>
}

export default App;